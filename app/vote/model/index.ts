export enum SwipeDirection {
  LEFT = "left",
  RIGHT = "right",
  UP = "up",
  NONE = "none",
}

export enum CategoryChoiceState {
  LIKE = "like",
  DISLIKE = "dislike",
  HOLD = "hold",
  PENDING = "pending",
}

export enum VoteType {
  VOTE_PENDING = "pending",
  VOTE_HOLD = "hold",
  VOTE_DONE = "done",
  NONE = "none",
}
